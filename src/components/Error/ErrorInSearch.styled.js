import styled from 'styled-components';

export const StyledError = styled.span`
  font-style: normal;
  font-weight: 700;
  font-size: inherit;
  position: absolute;
  right: 140px;
  color: #f74646;
`;
